<UserControl x:Class="HandWritten_OCR.Views.ImageListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:HandWritten_OCR.Converters"
             xmlns:vm="clr-namespace:HandWritten_OCR.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="200">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:BoolToVisibilityConverter x:Key="InvBoolToVis" IsInverted="True"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header bar -->
        <Border Grid.Row="0"
                Background="{DynamicResource SecondaryRegionBrush}"
                BorderBrush="{DynamicResource BorderRegionBrush}"
                BorderThickness="0,0,0,1" Padding="10,8">
            <DockPanel>
                <TextBlock Text="{Binding ImageList.Count, StringFormat='Images ({0})'}"
                           FontSize="13" FontWeight="Medium"
                           Foreground="{DynamicResource PrimaryTextBrush}"
                           VerticalAlignment="Center" DockPanel.Dock="Left"/>
                <Button Content="📂"
                        ToolTip="Open Folder"
                        DockPanel.Dock="Right"
                        HorizontalAlignment="Right"
                        Command="{Binding OpenFolderCommand}"
                        FontSize="14"
                        Padding="6,2"/>
            </DockPanel>
        </Border>

        <!-- Empty state -->
        <StackPanel Grid.Row="1"
                    VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="{Binding HasImageList, Converter={StaticResource InvBoolToVis}}">
            <TextBlock Text="📁" FontSize="28"
                       HorizontalAlignment="Center" Margin="0,0,0,8"/>
            <TextBlock Text="Open a folder"
                       FontSize="12"
                       Foreground="{DynamicResource SecondaryTextBrush}"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="to load images"
                       FontSize="12"
                       Foreground="{DynamicResource ThirdlyTextBrush}"
                       HorizontalAlignment="Center" Margin="0,2,0,0"/>
        </StackPanel>

        <!-- Image list -->
        <ListBox Grid.Row="1"
                 ItemsSource="{Binding ImageList}"
                 SelectedItem="{Binding SelectedImageItem, Mode=TwoWay}"
                 Background="Transparent"
                 BorderThickness="0"
                 Visibility="{Binding HasImageList, Converter={StaticResource BoolToVis}}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding FileName}"
                               FontSize="12"
                               TextTrimming="CharacterEllipsis"
                               ToolTip="{Binding FileName}"
                               Padding="2,3"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="8,4"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</UserControl>
